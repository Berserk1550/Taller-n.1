"use strict";
//Leer un numero y escribir el valor absoluto del mismo

let x=0;

x=parseInt(prompt("ingrese el valor"));

x = Math.abs(x);

document.write("el valor absoluto del valor ingresado es ",x,);